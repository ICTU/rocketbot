# ICTU ISD automation bot

## Build
The build process assumes you have Docker installed.

Clone this image, cd into the project dir and run:
```
./build.sh
```

## Start
With Docker Compose:

```
docker-compose up -d
```
