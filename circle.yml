machine:
  services:
    - docker

dependencies:
  override:
    - echo "no install"

test:
  override:
    - echo "no tests!"

deployment:
  hub:
    branch: master
    commands:
      - ./build.sh
      - docker push ictu/rocketbot
